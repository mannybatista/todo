define(['backbone'], function(Backbone) {
	// This model is just used to bind to the 'add view'. We'll pull the data and add it to the collection/hello_world.
	return Backbone.Model.extend({
	});
});
